# Textbutler app icon

Original geometric artwork in `icon.svg`, authored for Textbutler. Braces frame a small speech bubble. `icon.png` is its 256 × 256 raster export, generated with the repository's installed Sharp renderer. No external image, logo, or private asset is included.
